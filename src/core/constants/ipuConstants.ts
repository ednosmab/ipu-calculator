/**
 * Constants used in the IPU (Index of Polyurethane Units) calculations.
 */
export const IPU_CONSTANTS = {
  /**
   * Reference divisor used in the IPU formula.
   * Formula: (iso + poliol) / REFERENCE_DIVISOR
   */
  REFERENCE_DIVISOR: 0.140,
} as const;
